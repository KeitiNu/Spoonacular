<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spoonacular</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <div class="title">
        <h1>
            <%= dataFromSpoonacular.title %>
        </h1>
    </div>

    <div class="wrapper">
        <div class="img">
            <img src="<%= dataFromSpoonacular.image %>" alt="<%= dataFromSpoonacular.title %>">
        </div>
        <div class="ingredients">
            <h3>Ingredients:</h3>

            <ul>
                <% for(let i=0; i < dataFromSpoonacular.extendedIngredients.length; i++) { %>
                    <li>
                       <b><%= dataFromSpoonacular.extendedIngredients[i].name %>:</b> <%=
                                dataFromSpoonacular.extendedIngredients[i].measures.metric.amount %>
                                <%= dataFromSpoonacular.extendedIngredients[i].measures.metric.unitShort %>

                    </li>
                    <% } %>
            </ul>
        </div>
    </div>

    <h3>Summary:</h3>
    <div>
        <%- dataFromSpoonacular.summary %>
    </div>
    <h3>Instructions:</h3>
    <div>
        <%- dataFromSpoonacular.instructions %>
    </div>

</body>

</html>