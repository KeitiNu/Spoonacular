<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>
        <%= dataFromSpoonacular.title %>
    </h1>
    <div>
        <img src="<%= dataFromSpoonacular.image %>" alt="<%= dataFromSpoonacular.title %>">
    </div>
    <h3>Ingredients:</h3>
    <div>
        <ul>
            <% for(let i = 0; i < dataFromSpoonacular.extendedIngredients.length; i++) { %>
                <li>
                    <%= dataFromSpoonacular.extendedIngredients[i].name %>: <%= dataFromSpoonacular.extendedIngredients[i].amount %> <%= dataFromSpoonacular.extendedIngredients[i].unit %>

                </li>
                <% } %>
        </ul>
    </div>
    <h3>Summary:</h3>
    <div>
        <%- dataFromSpoonacular.summary %>
    </div>
    <h3>Instructions:</h3>
    <div>
        <%- dataFromSpoonacular.instructions %>
    </div>

</body>

</html>